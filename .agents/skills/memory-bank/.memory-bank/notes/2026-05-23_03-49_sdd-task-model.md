---
type: note
tags: [sdd, parser, validator, sprint-closeout, work-items]
related_features: [sdd-unification]
sprint: 1
importance: high
created: 2026-05-23
---

# sdd-task-model — Sprint 1 closeout

**Phase:** sdd-unification (3 sprints). **Sprint 1 of 3 — DONE.**

**Closed:** Stages 1-5.
- parser: `scripts/mb_work_items.py` (stdlib, JSONL CLI, 296 lines)
- generator: `scripts/mb-sdd.sh` emits `<!-- mb-task:N -->`
- validator: `scripts/mb-spec-validate.sh` (6 checks, --json mode)

**Lessons:**
- Two marker formats (`mb-stage` / `mb-task`) share one parser; `kind` field discriminates downstream consumers.
- Validator deliberately exits 2 on usage errors so callers can distinguish "spec broken" from "command misused".
- `--json` mode kept structurally identical to mb-rules-check JSON (`{"violations": [...]}`).

**Next:** Sprint 2 `sdd-work-engine` (6 stages) — wires `/mb work` to `mb_work_items.py`, deletes role-detection dup from `mb-work-plan.sh`.
